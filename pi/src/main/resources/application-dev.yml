server:
  port: 10201

spring:
  application:
    name: blog-pi

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/blog_pi?allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=utf-8&useSSL=false&allowMultiQueries=true&serverTimezone=GMT%2B8
    username: root
    password: root

# 自定义参数
netty:
  # 树莓派类服务器设备注册码，需要在平台上申请
  deviceCode: 2ecfb95116de4967afe7710e11ac00b4
